// Legacy page — superseded by ProductListPage + ProductDetailsPage routes.
// Kept as a stub to avoid import errors if referenced elsewhere.
import { Text } from '@mantine/core';
const ProductsPage = () => <Text>Используйте /admin/products</Text>;
export default ProductsPage;
